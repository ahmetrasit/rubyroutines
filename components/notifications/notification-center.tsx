'use client';

// DISABLED: Notification table not in schema yet
// This component will be enabled once the Notification model is added to the Prisma schema

interface NotificationCenterProps {
  roleId: string;
  userId: string;
}

export function NotificationCenter({ roleId, userId }: NotificationCenterProps) {
  // DISABLED: Notification table not in schema yet
  return null;
}
